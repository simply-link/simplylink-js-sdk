class BaseModel {
    constructor () {}

    /**
     * Parse data from resource to an object
     * @param resource
     * @return {*}
     */
    parseDataFromResourceServer (resource) {}
}

export default BaseModel
